{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ comment.content }}</td>
            </tr>
            <tr>
                <th>Date_cmt</th>
                <td>{{ comment.dateCmt ? comment.dateCmt|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comment_back_index') }}">back to list</a>

    <a href="{{ path('app_comment_back_edit', {'id': comment.id}) }}">edit</a>

    {{ include('comment_back/_delete_form.html.twig') }}
{% endblock %}
