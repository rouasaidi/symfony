<form method="post" action="{{ path('app_b_feedback_don_delete', {'id': feedback_don.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ feedback_don.id) }}">
    <button class="btn">Delete</button>
</form>
